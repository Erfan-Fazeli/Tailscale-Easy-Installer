# ================================
# TAILSCALE AUTO-INSTALLER CONFIG
# ================================

# REQUIRED: Tailscale Authentication Key
# Generate a pre-approved exit node auth key from: https://login.tailscale.com/admin/settings/keys
# Make sure to enable "Pre-approved" and "Exit node" options when creating the key
TAILSCALE_AUTH_KEY=tskey-auth-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# OPTIONAL: Custom hostname prefix
# If not set, will use default format: COUNTRY-NUMBER (e.g., US-1, FR-2)
# If set, format will be: PREFIX-COUNTRY-NUMBER (e.g., myserver-US-1)
HOSTNAME_PREFIX=

# OPTIONAL: Override country code detection
# If set, will use this instead of auto-detecting from IP
# Examples: US, FR, DE, GB, CA, etc.
COUNTRY_CODE_OVERRIDE=

# OPTIONAL: HTTP Health Server Port
# Default: 8080
# Used for health checks and keeping container alive on platforms like Render
HTTP_PORT=8080

# OPTIONAL: Enable detailed logging
# Default: true
# Set to false for minimal output
ENABLE_LOGGING=true

# OPTIONAL: Connection retry settings
# Maximum number of retry attempts for Tailscale connection
MAX_RETRIES=5

# OPTIONAL: Country detection timeout (seconds)
# Default: 5
COUNTRY_LOOKUP_TIMEOUT=5
