{
	"name": "Tailscale Exit Node",
	"build": {
		"dockerfile": "Dockerfile",
		"context": "."
	},
	"runArgs": [
		"--cap-add=NET_ADMIN",
		"--cap-add=NET_RAW",
		"--cap-add=SYS_MODULE"
	],
	"mounts": [
		"source=/dev/net/tun,target=/dev/net/tun,type=bind"
	],
	"containerEnv": {
		"CODESPACES": "true"
	},
	// Auto-start Tailscale when Codespace opens
	"postStartCommand": "nohup sudo -E /workspaces/Tailscale_AutoNode-Setup/start.sh > /tmp/tailscale.log 2>&1 & sleep 3 && printf '\n🚀 Tailscale started! View logs: tail -f /tmp/tailscale.log\n\n'",
	"forwardPorts": [8080],
	"portsAttributes": {
		"8080": {
			"label": "Health Check",
			"onAutoForward": "notify"
		}
	}
}
